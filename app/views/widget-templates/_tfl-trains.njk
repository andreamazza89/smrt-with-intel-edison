<ul>
  {% for line, enabled in widget.settings %}
  {# {{ line }}
  {{ enabled }}
  {{ widget.settings[line] }} #}
  {% if enabled %}
    <li class="flex-container" id="{{ line }}">
      <span class="tube-status" data-line="{{ line }}">Good service</span>
       on the
      <span class="tube-title tube-{{ line }}">{{ line }} line</span>
    </li>
    {% endif %}
  {% endfor %}
<ul>
